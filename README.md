<h1>📖 Инцидентный Трекер API</h1>

<h2>🚀 Запуск приложения</h2>
<p>Чтобы запустить API-сервис для учёта инцидентов, выполните следующие шаги:</p>
<ol>
    <li><strong>Клонируйте репозиторий</strong> или создайте новую папку для проекта.</li>
    <li><strong>Установите необходимые библиотеки:</strong>
        <pre><code>pip install fastapi uvicorn sqlalchemy</code></pre>
    </li>
    <li><strong>Запустите приложение</strong> с помощью следующей команды:
        <pre><code>uvicorn main:app --reload</code></pre>
    </li>
</ol>
<p>Сервис будет доступен по адресу: <a href="http://127.0.0.1:8000">http://127.0.0.1:8000</a>.</p>

<h2>📡 Эндпоинты API</h2>

<h3>1. Создание инцидента</h3>
<ul>
    <li><strong>Метод:</strong> <code>POST</code></li>
    <li><strong>URL:</strong> <code>/incidents/</code></li>
    <li><strong>Тело запроса:</strong> 
        <pre><code>{
    "description": "Описание инцидента",
    "status": "open",
    "source": "источник"
}</code></pre>
    </li>
    <li><strong>Ответ:</strong> Возвращает созданный инцидент с его ID и временной меткой.</li>
</ul>

<h3>2. Получение списка инцидентов</h3>
<ul>
    <li><strong>Метод:</strong> <code>GET</code></li>
    <li><strong>URL:</strong> <code>/incidents/</code></li>
    <li><strong>Параметры (по желанию):</strong> <code>status</code> (например, <code>open</code>, <code>resolved</code> и т.д.)</li>
    <li><strong>Ответ:</strong> Возвращает список инцидентов, соответствующих указанному статусу (по умолчанию возвращает все инциденты).</li>
</ul>

<h3>3. Обновление статуса инцидента</h3>
<ul>
    <li><strong>Метод:</strong> <code>PUT</code></li>
    <li><strong>URL:</strong> <code>/incidents/{incident_id}</code></li>
    <li><strong>Тело запроса:</strong>
        <pre><code>{
    "status": "resolved"
}</code></pre>
    </li>
    <li><strong>Ответ:</strong> Возвращает обновленный инцидент. Если инцидент не найден, возвращает ошибку 404.</li>
</ul>

<h2>🎯 Примеры использования</h2>
<ul>
    <li><strong>Создание инцидента:</strong>
        <pre><code>curl -X POST "http://127.0.0.1:8000/incidents/" -H "Content-Type: application/json" -d '{"description": "Проблема с сервером", "status": "open", "source": "система"}'</code></pre>
    </li>
    <li><strong>Получение всех инцидентов:</strong>
        <pre><code>curl -X GET "http://127.0.0.1:8000/incidents/"</code></pre>
    </li>
</ul>

<p>Теперь вы можете удобно управлять инцидентами, используя API вашего приложения!</p>

